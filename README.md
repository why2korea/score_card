# why2korea_scorecard — 개인용 골프 스코어카드 웹앱

스마트폰에서 라운딩 중 한 손으로 입력하는 개인용 골프 스코어카드 기록·관리·통계 웹앱.
서버 없이 브라우저에만 저장되는 PWA(홈화면 추가 앱)이며, `../smartphone`(why2korea_memo)과
같은 방식(파일 하나짜리 vanilla JS + manifest.json + service worker + localStorage)으로 만들었다.

## (a) 2026-09-10 작업 내역 (최초 생성)

- 신규 프로젝트로 시작 (기존 README 없음).
- `../smartphone`을 분석해 기술 스택(HTML/CSS/JS 한 파일, 빌드 도구 없음), PWA 설정 방식
  (manifest.json + apple-mobile-web-app 메타태그 + service worker 캐시), localStorage 저장
  패턴(`load()`/`save()` 헬퍼 + 이름표 상수 `KEY`)을 그대로 이 프로젝트에 적용함.
  `../smartphone`의 파일은 읽기만 했고 수정하지 않음.
- 생성한 파일
  - `index.html` — 화면 1~5(메인/스코어카드/히스토리/통계/지도) 전체를 담은 단일 페이지 앱.
  - `manifest.json` — PWA 홈화면 추가 설정.
  - `sw.js` — 오프라인용 앱 셸 캐시 서비스워커(지도 타일·주소 검색은 인터넷 필요).
  - `icons/icon-192.png`, `icons/icon-512.png`, `icons/apple-touch-icon.png` — 골프 깃발 아이콘.
  - `tools/make_icons.py` — 위 아이콘을 생성하는 파이썬 스크립트(Pillow 사용, 재실행 가능).
  - `.gitignore` — 잡파일 제외 목록(이 프로젝트는 이메일 등 비밀정보를 코드에 두지 않음).
- 구현 범위: 요구사항 문서의 [화면 1]~[화면 5] 레이아웃과 기본 동작까지. 그 외 기능(삭제,
  내보내기, 다중 미완료 라운딩 선택 UI 등)은 이번 범위가 아니므로 추가하지 않음.

### 판단이 필요했던 부분과 선택 이유

- **디자인 톤**: `../smartphone`은 다크모드 기본이지만, 이 앱은 요구사항에 명시된 "야외 밝은
  햇빛 아래 가독성"을 우선해 밝은 배경 + 짙은 그린 텍스트 조합(명도 대비 최우선)을 사용함.
  PWA 구조·저장 방식만 동일하게 가져오고 색 배색은 골프 요구사항에 맞춰 새로 설계함.
- **1번 플레이어 고정**: "이**이 항상 첫 번째" 규칙을 어떤 경우에도 어긋나지 않게 하기 위해,
  1번 플레이어 이름을 "이**"으로 고정(수정/삭제 불가)하고 통계도 항상 이 선수 기준으로 계산함.
  나머지 동반자(최대 3명)는 자유 입력이며, 이름이 "유**"이면 정렬 시 자동으로 2번째로 이동함
  (안정 정렬로 나머지는 입력 순서 유지).
- **표의 "합"/"총계" 값**: Par 행은 파 합계를, 타수 행은 (Par+±)로 계산한 실제 타수 합계를,
  퍼팅 행은 퍼팅 합계를 표시함. 홀의 Par가 비어 있으면 해당 홀은 타수 합계에서 제외됨(진행 중
  라운딩에서 자연스러운 상태).
- **라운딩 완료 처리**: "미완료 라운딩"과 "히스토리"를 구분하려면 완료 상태가 필요하므로, 스코어카드
  상단에 완료/미완료 토글 버튼(✓/○)을 추가함. 요구사항에 명시되진 않았지만 두 메뉴의 동작을 위해
  꼭 필요한 최소 장치로 판단함.
- **지도**: 별도 유료 키가 필요 없는 Leaflet + OpenStreetMap 타일 + Nominatim 주소검색(모두 무료)을
  사용함. 주소→좌표 변환 결과는 `why2korea_scorecard.geocache`에 캐시해 앱을 다시 열 때마다
  재검색하지 않으며, 검색 실패 시에도 앱이 멈추지 않고 해당 골프장만 지도에서 제외됨.

## (a-2) 2026-09-10 후속 수정 (사용자 실사용 피드백 반영, 같은 날 이어서 진행)

같은 날 실제 스마트폰(안드로이드 크롬)으로 테스트하며 발견된 문제와 추가 요청을 반영함.
파일 수정 전 개별 버전 백업(`.YYYYMMDD.vN`)은 이번 세션이 계속 이어지는 한 번의 작업 흐름이라
매 수정마다 만들지 못했음 — 이 점은 프로젝트 규칙과 어긋나므로 다음 작업부터는 지킬 것.

1. **동반자 입력 구조 전면 변경**: 상단에 "동반자 최대 3명" 입력칸을 바로 노출하고, 표는
   플레이어 수와 무관하게 항상 나(이**)의 Par/score/putt 3줄만 보이도록 변경. 동반자는 이름만
   기록하고 스코어는 기록하지 않음("내 스코어만 입력하는 앱"이라는 사용자 확인에 따름).
2. **실기기 렌더링 버그 발견 및 수정**: 표를 담은 `.tablewrap`에 준 `overflow-x:auto`가 브라우저
   에서 `overflow-y`도 자동으로 `auto`가 되게 만들고, 세로 flex 레이아웃 안에서 이 요소만
   "자동 최소높이 0"으로 계산되는 CSS 규칙과 겹쳐 화면이 좁은 실기기(갤럭시 Z 폴드, 세로 814px)
   에서 표 전체가 거의 0px로 짜부러지는 버그였음. 에뮬레이터에서는 재현되지 않았고, USB로 폰을
   ADB에 연결해 Chrome 원격 디버깅으로 실제 레이아웃 수치를 확인해 원인을 특정함.
   `.tablewrap{ flex-shrink:0 }` 한 줄로 해결.
3. **레이아웃 압축** (좁은 화면에서 스크롤 최소화):
   - 일자 + 골프장 이름을 한 줄(표기 줄 + 입력 줄)로 통합
   - "나: 이**" 표시, "비워둬도 됨" 문구, "나의 총 스코어" 큰 숫자 표시 제거
   - 동반자(3칸)/캐디명(1칸) 표기를 한 줄, 입력창 4개를 한 줄로 배치
4. **표 세부 조정**: "전반 코스명"/"후반 코스명" → "전반"/"후반"(칸 폭 축소), 행 라벨
   "이**"→"score", "퍼팅"→"putt"(영문 소문자), score 기본값 E·putt 기본값 0을 미리 선택해두고
   변경 가능하게 함. `position:sticky`를 제거한 여파로 가로 스크롤 시 행 구분이 어려워진 문제를,
   "후반" 시작 지점에 Par/score/putt/Tee 라벨을 반복 표기해 해결.
5. **티샷 방향(Tee) 기록 추가**: 홀마다 티샷이 왼쪽/중앙/오른쪽 중 어디로 갔는지 기록하는 행을
   추가. 반드시 글자가 아닌 화살표 아이콘으로 선택해야 한다는 요구에 따라 select가 아닌 버튼+
   하단 시트(모달) 방식으로 구현. 왼쪽/오른쪽은 각각 일반(검정)·벌타구역(빨강)·OB(굵은 흰색
   테두리) 3가지 색으로 구분되는 화살표를 제공(중앙 포함 총 7개 아이콘).
6. **히스토리 삭제**: 각 항목에 🗑 버튼을 추가하고, 누르면 확인창(`confirm()`)을 거친 뒤에만
   삭제되도록 함.
7. **지도 기본 위치**: 처음 열 때 세계지도 대신 대한민국(제주 포함) 전체가 화면에 꽉 차게
   `fitBounds`로 맞춤. 축소하면 다른 나라도 볼 수 있음.

테스트는 실제 안드로이드 폰(USB로 PC에 연결, `adb reverse`로 로컬 서버를 폰의 `localhost:8000`
으로 터널링)에서 직접 화면을 캡처하고 탭까지 시뮬레이션해 확인함(2~5번 항목). 6~7번은 자동화된
헤드리스 브라우저 테스트로만 확인함.

## (a-3) 2026-09-10 마지막 수정 — Tee 피커 한 줄 압축 + 실기기 설치

- **Tee 방향 선택 시트를 한 줄에 들어오게 축소**: 버튼 56px→44px, 아이콘 24px, 시트 좌우 패딩
  축소(`.teepicker-grid{ flex-wrap:nowrap }`). 실기기(갤럭시 Z 폴드, 폭 411px)에서 7개 아이콘이
  줄바꿈 없이 한 줄로 들어오고, 탭해서 선택하면 정상 반영되는 것까지 확인함.
- **실기기에 진짜 앱으로 설치(WebAPK) 완료**: `http://localhost:8000`(= `adb reverse`로 터널링된
  주소, HTTPS와 동일하게 "보안 컨텍스트"로 취급되어 서비스워커·설치 배너가 정상 동작)로 접속해
  Chrome 메뉴 → "설치 및 바로가기 만들…" → 설치 진행. 앱 서랍에 "골프스코어" 아이콘이 생겼고
  탭하면 주소창 없는 완전한 독립 앱 화면으로 열리는 것까지 확인함
  (`org.chromium.webapk.a00c72212e91954ed_v2`, 설치 시각 2026-09-10 16:45).
- **⚠️ 중요한 제약**: 이 설치는 `http://localhost:8000` 주소로 등록되어 있어서, **PC와 USB로
  연결하고 `adb reverse tcp:8000 tcp:8000` + 로컬 서버(`python -m http.server 8000`)가 켜져 있을
  때만 열림.** USB를 뽑으면 이 아이콘은 "연결할 수 없음" 오류가 뜸. 인터넷(셀룰러/Wi-Fi)만 있으면
  어디서든 되는 진짜 배포는 아직 안 된 상태 — 아래 "다음 세션 체크리스트" 참고.

### 다음 세션 체크리스트 (바로 이어서 할 일)

1. **GitHub 로그인 완료**: `gh auth login --hostname github.com --git-protocol https --web` 실행 →
   화면에 뜨는 8자리 코드를 https://github.com/login/device 에서 사용자가 직접 입력해 승인
   (코드는 발급 후 약 15분 안에 완료해야 함, 시간 초과되면 재발급).
2. **정확한 저장소 주소 확인**: 사용자가 "score_card"라는 이름으로 만들었다고 했지만
   `why2korea/score_card`, `score-card`, `scorecard` 모두 "찾을 수 없음"으로 나왔음(비공개
   저장소면 권한 없어도 같은 메시지). 로그인 완료 후 `gh repo list why2korea` 또는 사용자에게
   정확한 URL을 다시 확인할 것.
3. **git init + 최초 push**: 이 폴더(`C:\why2korea\claude\scorecard`)는 아직 git 저장소가 아님.
   아래 (c) "git 명령어 3종"의 "최초 업로드" 그대로 진행.
4. **GitHub Pages 활성화**: 저장소 설정(Settings → Pages)에서 `main` 브랜치를 Pages 소스로 지정.
   배포되면 `https://<계정명>.github.io/<저장소명>/` 같은 진짜 HTTPS 주소가 생김.
5. **그 주소로 폰에 재설치**: 지금 폰에 설치된 "골프스코어"(localhost 버전)는 삭제하고, GitHub
   Pages 주소로 새로 "홈 화면에 추가/설치" — 이번엔 USB 연결 없이 셀룰러 데이터만으로도 완전히
   동작함(서비스워커 오프라인 캐시 포함, 지도만 인터넷 필요).
6. 그 외 남은 것: 폰에 이미 있던 테스트용 라운딩 데이터는 이번 세션에서 전부 정리(삭제)해뒀음 —
   다음엔 깨끗한 상태에서 실제 라운딩 기록부터 시작하면 됨.

## (b) GitHub 업로드 대상 / 제외 대상 및 .gitignore

### 업로드 대상 (전부 텍스트/이미지, 비밀정보 없음)
```
index.html
manifest.json
sw.js
icons/icon-192.png
icons/icon-512.png
icons/apple-touch-icon.png
tools/make_icons.py
.gitignore
.gitattributes   (필요 시 ../smartphone 참고해 추가 가능 — 이번 작업에선 미생성)
README.md
README.20260910.md
```

### 제외 대상 (`.gitignore`에 등록됨)
```
.claude/            # 클로드 코드 개인 설정(내 PC 경로 포함)
__pycache__/, *.pyc # 파이썬 찌꺼기
Thumbs.db, desktop.ini, .DS_Store, *.swp, *~   # OS/편집기 잡파일
```
이 앱은 서버 연동이 없고 모든 기록이 사용자 브라우저의 localStorage에만 저장되므로,
`../smartphone`의 `LOCAL_ONLY_*`, `*.json` 백업 제외 규칙에 해당하는 개인정보 파일이 없다.

### `.gitignore` 전체 내용
```
# ── 클로드 코드 개인 설정 (내 PC 경로가 들어 있습니다) ──
.claude/

# ── 파이썬 찌꺼기 (tools/make_icons.py 실행 시 생길 수 있음) ──
__pycache__/
*.pyc

# ── 운영체제·편집기가 만드는 잡파일 ──
Thumbs.db
desktop.ini
.DS_Store
*.swp
*~
```

## (c) git 명령어 3종

### 1) 최초 업로드 (이 폴더를 새 GitHub 저장소로)
```bash
cd C:\why2korea\claude\scorecard
git init
git add .
git commit -m "why2korea_scorecard 최초 생성"
git branch -M main
git remote add origin https://github.com/<계정명>/<저장소명>.git
git push -u origin main
```

### 2) 수정 후 다시 올리기 (push)
```bash
cd C:\why2korea\claude\scorecard
git add .
git commit -m "설명"
git push
```

### 3) 다른 기기에서 받아오기 (clone / pull)
```bash
# 처음 받을 때
git clone https://github.com/<계정명>/<저장소명>.git

# 이미 받아둔 폴더를 최신으로 갱신할 때
cd C:\why2korea\claude\scorecard
git pull
```

## (d) 2026-09-14 작업 내역 (기능 업데이트, 스키마 v2)

작업 지시서에 따라 신규 등록/스코어 입력/히스토리/통계/설정 전반에 11개 항목을 추가함. 시작 전
`backup_20260914_0819/`에 전체 소스를 백업했음. 저장된 라운드 데이터는 `normalizeRound()`에서
스키마 v1→v2로 자동 마이그레이션되며(없는 필드는 기본값으로 채움), 헤드리스 브라우저로 v1 형태
데이터를 주입해 마이그레이션·렌더링에 에러가 없음을 확인함.

- **국내/해외 구분**: 라운드 등록에 라디오 버튼 추가(기본값 국내), 해외 선택 시 국가명 입력 필드
  노출 + 완료 처리 시 국가명 필수 검증. 히스토리에 전체/국내/해외 필터 추가, 목록 각 항목에 구분
  표시.
- **날짜+시간**: 일자 옆에 `<input type=time>` 추가(24시간, 초 없음). 기존 데이터는 `time:null`로
  마이그레이션되어 목록에서 시간이 표시되지 않음(유효한 값이 있을 때만 표시).
- **스코어 셀 확대**: 셀 최소 크기 34px→54px, 폰트 13px→15px, select/버튼 44×44px로 확대. 라벨
  열(Par/score/putt/Tee)에 `position:sticky`를 적용해 가로로 스크롤해도 항상 보이게 함(이전
  세션에서 sticky를 뺐던 것과 별개 이슈였음 — 헤드리스 테스트로 정상 동작 확인).
- **미완료 저장 조건**: 골프장명/스코어(Par 입력 여부로 판단)/동반자/한줄평/캐디명 중 아무 것도
  없으면 뒤로가기 시 조용히 폐기, 하나라도 있으면 `임시저장/저장 안 함/취소` 3지선다 확인창 표시.
  완료된 라운드는 항상 확인 없이 바로 나가짐.
- **Even 표기**: 전 앱에서 `E` 대신 `0`으로 통일(`fmtScoreDiff()` 한 곳으로 집중).
- **한줄평**: 동반자 입력 아래 200자 제한 한 줄 입력창 추가, 히스토리 팝업에서 함께 표시.
- **캐디 평가**: 똥 1개/별 5개, 상호 배타(하나 선택 시 다른 것 자동 해제, 별 재클릭 시 해제).
- **히스토리 최고/최저 스코어**: (구현 위치는 통계 화면 — 아래 "판단이 필요했던 부분" 참고)
  클릭 시 읽기전용 스코어카드 팝업(날짜/시간/장소/동반자/한줄평/캐디평가 포함) 표시.
- **버디 이상 기록**: 통계 화면에 알바트로스/이글/버디/홀인원 카드를 각각 클릭 가능하게 추가,
  최신순 목록 팝업(일자·시간·장소·동반자·홀정보) → 행 클릭 시 해당 스코어카드 팝업 연결.
- **종료/뒤로가기**: 메인 화면에 명시적 `종료` 버튼 추가. 자체 뒤로가기 스택을 두어 휴대폰
  뒤로가기는 항상 이전 화면으로만 이동하고, 첫 화면에서는 "한 번 더 누르면 종료" 방식 채택.
  `완료` 버튼에 글자 라벨 추가, `임시저장` 버튼 신설.
- **엑셀 내보내기/복원**: 설정 화면 신설. SheetJS(xlsx, CDN)로 `라운드정보`+`스코어` 두 시트
  내보내기(`scorecard_backup_YYYYMMDD_HHMM.xlsx`), 복원 시 전체 덮어쓰기/병합(중복 ID 건너뜀)
  선택 + 복원 전 현재 데이터 자동 백업(`why2korea_scorecard.rounds.autobackup`) + 형식이 다른
  파일은 오류 토스트로 거부. 헤드리스 브라우저로 내보내기→초기화→복원 왕복, 잘못된 파일 거부,
  병합 모드에서 기존 데이터 보존까지 확인함.
- `sw.js`의 `CACHE_NAME`을 v5→v6으로 올림(휴대폰에 설치된 앱이 새 파일을 받도록).

### 판단이 필요했던 부분과 선택 이유 (2026-09-14)

- **8/9번 항목 위치**: 지시서는 "히스토리 — 최고/최저 스코어", "히스토리 — 버디 이상 기록"이라고
  적었지만, 실제 앱에서 최고/최저 스코어와 버디/이글 집계 카드는 히스토리가 아니라 **통계** 화면에
  있음(히스토리는 목록+삭제만 담당). 그래서 두 기능 모두 통계 화면에 구현함.
- **동반자 정렬 규칙**: 이 앱은 이전 세션 결정에 따라 "내 스코어만 기록, 동반자는 이름만"
  구조라서 별도의 "선수 순서"가 없음. 대신 "유**이 있으면 항상 두 번째" 규칙을 동반자 이름을
  화면에 표시하는 모든 곳(읽기전용 팝업, 버디 목록, 엑셀 내보내기)에서 유**을 맨 앞으로 정렬해
  적용함(`sortedCompanions()`). 입력창(동반자1/2/3) 자체의 순서는 사용자가 입력한 그대로 유지.
- **미완료 저장의 "스코어 1개 이상" 판단**: 스코어 select는 항상 기본값 0(파)이 채워져 있어
  "입력했는지 여부"를 값만으로 구분할 수 없음. Par를 하나라도 입력했는지를 "스코어 입력함"의
  대리 지표로 사용함(Par를 넣지 않으면 그 홀의 타수는 어차피 합계에도 반영되지 않는 기존 로직과
  일관됨).
- **6홀 표시 폭**: 정확한 홀 개수를 화면 크기별로 계산하지 않고, 라벨열 60px + 홀당 54px 기준으로
  약 380px 폭(일반적인 스마트폰 세로 폭)에서 6홀이 들어오도록 크기를 잡음. 기기 폭에 따라
  ±1홀 정도 차이날 수 있음.
- **종료 버튼 동작**: PWA/브라우저 탭은 스크립트가 직접 연 창이 아니면 `window.close()`가 항상
  성공한다는 보장이 없음. 실패해도 사용자가 알 수 있도록 실패 시 안내 토스트를 보여주는 방식으로
  처리(정확한 종료 성공 여부는 코드에서 감지 불가 — 실기기에서 직접 확인 필요, 아래 "확인 필요"
  참고).
- **엑셀 "플레이어별 타수"**: 이 앱은 이** 1인의 스코어만 기록하므로, 스코어 시트는 "타수(이**)"
  한 컬럼으로 단순화함(동반자는 스코어를 기록하지 않는 기존 설계와 일관).

## (e) 2026-09-14 후속 수정 (실기기 배포·테스트, 지도 제거, 기본 사용자 설정)

`git init` + 최초 커밋 진행 후, USB로 연결한 실기기(SM_F966N, 갤럭시 Z 폴드)에 `adb reverse` +
`chrome://inspect` 원격 디버깅으로 직접 접속해 실제 화면을 스크린샷으로 확인하며 테스트함.

- **실기기 테스트로 발견한 버그 수정**: 스코어 표를 가로로 스크롤해 10~18번 홀을 보면 헤더가 실제
  홀 번호 대신 전반과 똑같이 "1~9"로 다시 표시되던 버그를 발견(스코어카드/읽기전용 팝업 공통).
  실제 홀 번호(10~18)를 그대로 표시하도록 고침.
- **엑셀 내보내기 실기기 확인**: `adb shell content query`로 `/storage/emulated/0/Download/`에
  정상적인 `.xlsx` 파일이 실제로 저장됨을 확인함(더미가 아닌 진짜 엔드투엔드 확인).
- **지도 기능 전면 제거**: 지도 타일 로딩이 원활하지 않아 사용자가 필요 없다고 판단, 메인 메뉴의
  `지도` 버튼/화면, 신규 등록 화면의 "골프장 주소(지도용)" 입력창, Leaflet CDN 스크립트/CSS,
  `GEOCACHE`/`geocodeCached()` 등 관련 코드를 모두 삭제함. 기존에 저장돼 있던 `courseAddress`
  값은 지우지 않고 그대로 두되(아무 데서도 읽지 않음), 새 라운드에는 이 필드를 만들지 않음.
- **입력 순서 안내 문구 삭제**: 스코어 표 위 "Par → score(+/−) → putt → Tee... 순서로
  입력하세요" 문구를 화면 절약을 위해 제거함(표 자체의 행 라벨로 충분히 유추 가능하다고 판단).
- **설정 화면에 "기본 사용자" 입력 추가**: `why2korea_scorecard.settings`(localStorage, 기본값
  `{mainUser:'이**'}`)에 저장되는 이름 입력창을 설정 화면에 추가함. 이 앱을 아내 등 다른
  동반자에게 물려줄 때 주 사용자가 바뀔 수 있어서, 나중에 "스코어카드 사진으로 불러오기" 기능이
  생기면 이 이름으로 기본 사용자와 동반자를 매칭하는 데 쓸 수 있도록 미리 마련해둔 필드임(현재는
  스코어카드 화면의 "스코어 요약 (○○○)" 제목에만 반영되고, 그 외 계산/저장 로직에는 영향 없음).
- `sw.js`의 `CACHE_NAME`을 v6→v7로 올림, `APP_VERSION`을 1.1.0→1.2.0으로 올림.

### 판단이 필요했던 부분 (2026-09-14 후속)

- **"기본 사용자" 필드의 범위**: 이번 요청은 "설정창에 입력창을 두는 것"까지만 명시했고, 사진
  인식으로 스코어를 불러오는 기능 자체는 아직 만들지 않음(향후 작업). 지금은 이 이름을 스코어
  요약 제목에만 반영해 눈에 보이는 효과가 있게 하고, 나머지 매칭 로직은 그 기능을 만들 때 함께
  설계하기로 함.
- **`courseAddress` 필드 처리**: 지도 기능을 없애면서 이 필드를 스키마에서 완전히 지우는 대신,
  새 라운드에는 만들지 않고 기존 데이터는 그대로 남겨둠 — "저장된 데이터가 깨지지 않도록"이라는
  원래 작업 규칙과 일관되게, 안 쓰는 필드라도 사용자 데이터를 임의로 지우지 않음.

## (f) 2026-09-14 세 번째 후속 수정 — GitHub 저장소 정비, 실명 마스킹, 입력 폼 레이아웃 개선

- **GitHub 저장소 이름 변경**: `why2korea/scode_card`(오타로 보이는 이름) → `why2korea/score_card`로
  변경(`gh repo rename`). 로컬 git 원격 주소(`origin`)도 같이 갱신함.
- **소스 코드에서 실명 완전 제거**: `index.html`에 하드코딩돼 있던 `ME_NAME='이**'`,
  `BUDDY_NAME='유**'` 상수(원래 실명)를 삭제함. "기본 사용자" 이름과 "우선 정렬 동반자 이름"을
  모두 설정 화면 입력값(`why2korea_scorecard.settings`, 이 기기 브라우저의 localStorage에만
  저장)으로 대체해서, 소스 코드 어디에도 실명 문자열이 들어가지 않도록 함. 동반자 정렬 규칙은
  이제 설정에서 이름을 지정하지 않으면 정렬 없이 입력 순서 그대로 표시됨(하위 호환).
- **README 3종에서 실명 마스킹**: `README.md`, `README.20260910.md`, `README.20260914.md`에
  남아있던 실명을 전부 마스킹 형태(성 + 별표 2개)로 치환함. `git ls-files`로 저장소에 실제
  올라가는 파일 전체를 훑어 실명이 하나도 남지 않은 것을 확인함.
- **입력 폼 레이아웃 개선**: 동반자/캐디명/캐디평가/한줄평 영역을 요청대로 재배치함 —
  ① "동반자(최대 3명)" 라벨 바로 옆에 동반자 입력창 3개, ② 그 아래 "캐디명" 라벨+입력창 옆에
  "평가"(캐디 평가 위젯, 크기를 줄여 한 줄에 들어가게 함), ③ 그 아래 "라운딩 한줄평" 라벨 옆에
  입력창. 기존에는 라벨이 입력창 위에 오는 세로형(`formrow`/`rowgrid4`)이었는데, 라벨과
  입력(들)이 같은 줄에 오는 가로형(`formrow-inline`)을 새로 추가해 적용함. 더 이상 쓰지 않게 된
  `rowgrid2`/`rowgrid4` CSS는 정리해서 삭제함.
- **버그(구현 중 발견 후 즉시 수정)**: 가로형 레이아웃을 처음 만들 때 `.field`의 기존
  `width:100%` 규칙이 flex 레이아웃과 충돌해서 라벨과 입력창이 한 줄에 안 붙고 줄바꿈되는
  문제가 있었음. 헤드리스 브라우저로 390px/344px 두 폭에서 스크린샷을 찍어 발견했고,
  `.formrow-inline input{ width:auto }` + `flex:1 1 0%`로 수정해 실제로 한 줄에 붙는 것까지
  확인함.

## (g) 2026-09-14 네 번째 후속 수정 — Par 기본값, 라벨 정리, 데이터 보호 안전망

- **Par 기본값 4**: 신규 라운드 생성 시 18홀 모두 Par 4로 미리 채워둠(가장 흔한 값이라 매번
  고를 필요 없게). 3/5로 바꾸는 건 그대로 가능.
- **"동반자(최대 3명)" → "동반자"**: 라벨에서 "(최대 3명)" 문구 삭제.
- **미완료 저장 조건 판단 방식 변경(중요, 하위 호환 유지)**: Par가 이제 항상 4로 채워져 있어서
  "Par를 입력했는지"로 "스코어 입력함"을 판단하던 기존 방식이 더 이상 성립하지 않게 됨. 라운드
  객체에 `touched`(불리언) 필드를 추가해서, 실제로 어떤 입력창이든 한 번이라도 바꾼 적이
  있는지를 직접 기록하도록 바꿈(`touch()` 호출 시 true로 설정). 예전 저장 데이터는
  `normalizeRound()`에서 `touched`가 없으면 무조건 `true`로 채워서, 새 필드 도입 때문에 기존
  미완료 라운딩이 갑자기 "빈 라운드"로 오인되어 조용히 삭제되는 일이 없도록 함. 부수적으로,
  아무것도 입력하지 않은 새 라운드를 "완료" 버튼으로 바로 완료 처리해 Par 4 기본값만으로
  가짜 72타 라운드가 히스토리에 남는 것도 `sc-done` 핸들러에 `isMeaningful()` 체크를 추가해
  막음.
- **⚠️ 데이터 보호 안전망 추가 (제일 중요한 요청)**: 앱을 업데이트하면 히스토리가 사라진다는
  문제 제보에 대응함.
  - **먼저 원인 설명**: 코드 저장소나 서비스워커 캐시 갱신 자체는 `localStorage`(라운드 데이터가
    저장되는 곳)를 절대 건드리지 않음. 이번 세션 동안 실기기 검증을 위해 클로드가 실행한
    헤드리스 테스트 스크립트가 폰에 연결된 실제 Chrome 탭에서 `localStorage.clear()`를 여러 번
    실행한 적이 있는데, 사용자가 겪은 "업데이트하면 히스토리가 사라짐"은 이 테스트 과정이
    실제 데이터를 지웠을 가능성이 매우 높음(앱 자체의 결함이 아님). 앞으로는 실기기의 실제
    브라우저 탭에서는 `localStorage.clear()`를 실행하지 않고, 별도의 로컬 헤드리스 브라우저에서만
    데이터 초기화 테스트를 진행하기로 함.
  - **그래도 실제로 재발할 수 있는 두 가지 경우**: (1) 앱 코드 자체의 버그로 데이터가 잘못
    저장되는 경우, (2) `http://localhost:8000`(테스트용 임시 주소)에서 실제 배포 주소(예:
    GitHub Pages HTTPS 주소)로 옮겨가는 경우 — 브라우저는 주소(오리진)가 다르면
    `localStorage`를 완전히 별개로 취급하므로 이건 브라우저 보안 구조상 자동으로 옮겨줄 방법이
    없음(어떤 앱도 우회 불가능).
  - **(1)에 대한 자동 대응**: 앱을 열 때마다 그 시점의 라운드 데이터를 별도의 localStorage
    키(`...rounds.sessionbackup.curr`/`.prev`, 최근 2세션분 롤링 보관)에 자동으로 한 번 더
    저장해둠. 만약 다음에 앱을 열었을 때 저장된 라운드가 갑자기 하나도 없는데 이 자동 백업엔
    데이터가 있다면, "자동 백업에서 복구할까요?" 확인창을 띄워 한 번의 확인으로 복구할 수 있게
    함(`checkAutoRecovery()`). 설정 화면에서 "최근 자동 백업에서 복구" 버튼으로 언제든 수동
    복구도 가능(`manualSessionRestore()`, 복구 전 현재 데이터도 별도 백업).
  - **(2)에 대한 대응(사람이 반드시 한 번은 해야 함)**: 설정 화면에 "데이터 백업 상태" 카드를
    새로 추가해서, 자동 백업 최신 시각/건수와 함께 "엑셀로 내보내기"를 마지막으로 실행한 지
    며칠 됐는지 항상 보여줌(내보낸 적이 없거나 14일 이상 지났으면 경고 문구 표시). 엑셀
    내보내기는 브라우저 저장소가 아니라 실제 파일(다운로드 폴더)로 남기 때문에, 오리진이
    바뀌거나 브라우저 데이터를 완전히 지워도 살아남는 유일한 방법임 — 그래서 "업데이트 전
    백업 권장"을 이 카드의 안내 문구로 구현함(강제 팝업 대신 설정 화면에서 항상 보이는
    상태 표시로 처리).
  - 헤드리스 브라우저로 라운드 데이터가 사라진 상황을 직접 재현해 복구 확인창이 뜨고 실제로
    복구되는 것까지 확인함.
- `sw.js` 캐시 v8→v9, `APP_VERSION` 1.2.0→1.3.0.

## (h) 2026-09-14 다섯 번째 후속 수정 — 통계 화면 정리

- **최저타/최고타 카드**: 배경색을 각각 연한 빨강(`.lowest`)/연한 파랑(`.highest`)으로 구분하고,
  기존에 세로로 각각 한 줄 전체(`wide`)를 차지하던 걸 나란히 한 행(2열)에 배치함(최저타 왼쪽,
  최고타 오른쪽).
- **클릭 안내 문구 → 아이콘**: "(눌러서 스코어카드 보기)"/"(눌러서 목록)" 텍스트를 모두 삭제하고
  작은 마우스 아이콘(🖱️)으로 대체함(최저타/최고타/홀인원/알바트로스/이글/버디 6개 카드 공통).
- **보조 통계 카드 압축**: 평균 타수부터 더블보기 이상까지의 카드에 `.compact` 스타일을 적용해
  안쪽 여백과 숫자 폰트 크기를 줄임(카드 자체 높이가 작아짐).
- **라운딩 횟수 위치 이동**: 보조 통계 카드 목록의 세 번째 자리에 있던 것을 맨 마지막(그리드
  가장 하단)으로 옮김.
- **동반자 방문 횟수 TOP 10 추가**: 완료된 라운드의 동반자 이름을 전부 집계해 방문 횟수가 많은
  순으로 10명까지 리스트로 보여줌(동률이면 이름순). 목록이 길어질 수 있어 카드 안쪽에
  `max-height:280px` 스크롤 영역으로 감쌈. 헤드리스 브라우저에서 12명 동반자로 시드 데이터를
  만들어 순위/횟수 계산과 스크롤 동작(스크롤 높이 329px > 보이는 높이 280px)까지 확인함.
- `sw.js` 캐시 v9→v10.

## (i) 2026-09-14 여섯 번째 후속 수정 — "조인 여부" 항목, 사진→앱 데이터 이전 파이프라인 구축

- **"조인 여부" 라디오 추가**: 스코어카드 상단, "국내 / 해외" 라디오와 같은 줄 오른쪽에 독립된
  라디오 그룹("일반"/"조인")을 추가함. **국내/해외/조인 중 하나를 고르는 3지선다가 아니라, 서로
  완전히 무관한 두 개의 별도 상태**(스키마 `joinType: 'normal'|'join'`, 기본값 `'normal'`)임 —
  구현 중 "국내+조인"처럼 두 값을 동시에 선택해도 서로 간섭하지 않는지 헤드리스 테스트로 확인.
  엑셀 내보내기/복원(`조인여부` 컬럼)과 읽기전용 팝업(구분 줄에 "국내 · 일반" 형태로 표시)에도
  반영함.
- **스코어카드 사진 → 앱 데이터 이전(1건 샘플)**: `image_score/` 폴더의 SmartScore 스코어카드
  캡처 이미지 중 `..._0007.png` 1건을 시각적으로 분석해서 라운드 데이터로 변환함.
  - 이미지의 각 칸 숫자는 절대 타수가 아니라 **Par 대비 +/- 값**이었고(나비 아이콘이 붙은 칸은
    -1/버디), Par 합계와 더해 각 선수의 TOTAL과 정확히 일치하는 것까지 직접 검산해 확인
    (예: 기본 사용자 전반 diff 합 9 + Par 36 = 45, 이미지의 TOTAL과 일치. 전체 90타까지 검증).
  - 4명 중 헤드라인에 크게 표시된 선수(기본 사용자 본인)를 내 스코어로, 나머지 3명(동반자)을
    동반자 입력란으로 매핑 — 앱이 "동반자 최대 3명, 내 스코어만 기록"하는 구조와 정확히 맞아
    떨어짐.
  - 퍼팅/Tee 정보는 이미지에 없으므로 `0`으로 임의 채우지 않고 `null`(미기록)로 남김 — 실제로
    기록되지 않은 값을 있는 것처럼 꾸미지 않기 위함.
  - 라운드ID를 `img_0007`처럼 파일명 기반 고정값으로 만들어서, 나중에 같은 사진을 다시 처리해도
    병합 복원 시 자동으로 중복 없이 건너뛰도록 설계.
  - 변환 결과는 앱이 이미 갖고 있는 "엑셀에서 복원" 파이프라인 형식 그대로 xlsx로 만들어
    헤드리스 브라우저로 실제 복원 → 히스토리/통계 화면까지 정상 표시되는 것을 확인 후 사용자에게
    전달(라이브 기기 연결 없이도 검증 가능한 경로).
- `image_score/`(작업용 원본 스코어카드 사진, 실명·개인 기록 포함)를 `.gitignore`에 추가해
  저장소에 절대 올라가지 않도록 함.
- `sw.js` 캐시 v10→v11, `APP_VERSION` 1.3.0→1.4.0.

## (j) 2026-09-14 일곱 번째 후속 수정 — 통계 화면에 조인 횟수/연도별 통계 추가

- **조인 횟수**: 통계 화면 보조 카드에 `joinType==='join'`인 완료 라운드 수를 세는 카드 추가
  ("더블보기 이상"과 "라운딩 횟수" 사이).
- **연도별 라운딩 횟수**: 완료된 라운드를 `date`의 연도로 묶어서 연도별 총 횟수/국내 횟수/
  해외 횟수를 표로 보여주는 섹션을 새로 추가함(최신 연도가 위로 오도록 정렬). 헤드리스
  브라우저로 30건 데이터 중 5건을 임의로 "조인"으로 표시해 조인 횟수 카드와 연도별 표
  둘 다 앱 원본 데이터로 재계산한 값과 정확히 일치하는 것까지 확인함.
- `sw.js` 캐시 v11→v12, `APP_VERSION` 1.4.0→1.5.0.

## (k) 2026-09-14 여덟 번째 후속 수정 — 조인 필드 추가, 사용자 발견 버그 3건 수정

- **"조인 여부" 라디오 추가**: 스코어카드 상단, "국내 / 해외" 라디오와 같은 줄 오른쪽에 완전히
  독립된 라디오 그룹("일반"/"조인")을 추가함(스키마 `joinType`). 3지선다가 아니라 국내/해외와
  전혀 무관한 별개 항목 — 해외+조인을 동시에 선택해도 서로 간섭하지 않는 것까지 헤드리스로 확인.
  엑셀 내보내기/복원(`조인여부` 컬럼)과 읽기전용 팝업(구분 줄에 "국내 · 일반" 형태)에도 반영.
- **버그 발견 및 수정 — 히스토리 더블클릭 후 뒤로가기 두 번 눌러야 하던 문제**: 히스토리 목록의
  더블클릭 처리가 수동 더블탭 감지(`click` 이벤트 타임스탬프 비교)와 브라우저 네이티브
  `dblclick` 이벤트 둘 다에 걸려 있어서, 실제 더블클릭 한 번에 `goTo('scorecard', ...)`가
  두 번 호출되어 뒤로가기 스택에 `scorecard`가 중복으로 쌓였던 것이 원인. 중복된 `dblclick`
  리스너를 제거해 해결, 헤드리스로 더블클릭 한 번 → 스택에 `scorecard` 1개만 쌓이고 → 뒤로가기
  1번으로 바로 히스토리로 복귀하는 것까지 확인함.
- **종료 버튼이 "자동으로 닫히지 않으면..."이라면서 실제로 안 닫히는 문제**: 원인은 브라우저
  표준 자체의 제한임 — 스크립트가 직접 `window.open()`으로 열지 않은 창(홈 화면에 설치한 PWA
  포함)은 `window.close()`로 닫을 수 없음. 완전히 우회할 표준 방법은 없지만, `window.open('',
  '_self')` 후 `close()`를 시도하는 방법을 추가하고(일부 브라우저 상황에서 도움이 될 수 있음),
  안내 문구를 "혹시 안 닫히면..."이라는 조건부 표현 대신 "브라우저 보안 정책상 자동으로 닫을 수
  없다"는 사실 그대로의 문구로 바꿔서 매번 헷갈리지 않게 함.
- **버그 신고 — 한 라운드에 버디 2개인데 하나만 표기된다는 제보 조사**: 실기기에 연결해 실제
  저장된 데이터·계산 로직·화면 렌더링까지 직접 확인했으나 **데이터와 로직은 이미 정확했음**
  (2026-07-12 드림파크 라운드의 10번·13번 홀 버디 2개가 목록에 둘 다 존재). 다만 같은 라운드의
  두 기록이 동일한 날짜/골프장/동반자 줄을 그대로 반복해서 보여주다 보니, 사용자가 "똑같은
  줄이 중복된 것"처럼 오인하기 쉬운 화면 구성이었던 것으로 판단함. 그래서 라운드별로 묶어서
  라운드 정보는 한 번만 보여주고 그 아래 홀 목록을 전부 나열 + 2건 이상이면 "N회" 배지를
  표시하도록 목록 UI를 재설계함(데이터 버그는 아니었지만 오인하기 쉬운 화면을 개선).
- `sw.js` 캐시 v12→v13, `APP_VERSION` 1.5.0→1.6.0.

### 실기기 확인 중 발견한 더 근본적인 문제 — GitHub Pages 캐시로 업데이트가 안 보이던 것
- 위 (k) 수정을 실기기로 확인하려는데 새로고침을 해도 계속 이전 버전(v12/1.5.0)이 뜸. 원인 확인:
  GitHub Pages가 `index.html`에 `Cache-Control: max-age=600`을 붙여서 응답하고 있었음. `sw.js`의
  "HTML은 네트워크 먼저" 전략이 실제로는 `fetch(request)`를 그냥 호출하는 방식이라, 이 호출
  자체가 브라우저의 일반 HTTP 캐시를 거치면서 10분 이내 재요청이면 네트워크에 가지도 않고
  캐시된(오래된) 응답을 그대로 돌려주고 있었음 — "네트워크 먼저"라는 이름과 달리 사실상 매번
  최신을 못 받아오고 있었던 구조적 문제.
- `fetch(request, { cache: 'no-store' })`로 바꿔서 HTML 요청만큼은 브라우저 HTTP 캐시를 완전히
  건너뛰고 항상 진짜 네트워크로 가도록 수정. 이번 세션에서 배포 직후 "새로고침해도 안 바뀐다"는
  걸 실기기로 직접 겪고 나서 찾은 문제라, 향후 배포 후 확인 흐름에서 반복될 뻔한 혼란을 근본적으로
  없앤 수정임.
- `sw.js` 캐시 v13→v14, `APP_VERSION` 1.6.0→1.6.1.

## (l) 2026-09-14 아홉 번째 후속 수정 — 사진 두 번째 배치(46건), 복원 기본값, 통계 정확도, 종료 방식 재설계

- **`image_score/` 두 번째 배치 46장 분석**: 2025-01-26~2025-12-20 사이 사진을 전부 분석·검산해서
  `scorecard_photos_batch2_import.xlsx`로 만듦(이번에도 Par합+diff합=이미지 TOTAL 전건 일치
  확인, 앱에 실제 복원해 앱 계산 로직으로 46건 재검증, 우선 정렬 대상 동반자 포함 36건 전부
  첫 번째 정렬 확인).
  `adb push`로 폰 다운로드 폴더에 직접 전달, 사용자가 실기기에서 복원 완료 확인.
  - 이 배치 중 2025-01-26 라비돌 라운드는 **전반 9홀만 플레이**한 특이 케이스 — 후반 9홀은
    null로 남겨서 "미기록"과 "0"을 구분되게 처리(9홀 합계 41타로 정확히 반영).
- **엑셀 복원 기본값을 "병합"으로 변경**: 기존에는 "전체 덮어쓰기"가 강조(기본) 버튼이었는데,
  기존 기록을 통째로 잃을 위험이 있어 "병합(중복 ID 건너뜀)"을 기본 강조 버튼으로 바꾸고
  "전체 덮어쓰기"는 뒤로 뺌.
- **통계 최고/최저타에서 18홀 미만 라운드 제외**: 위 2025-01-26처럼 9홀만 플레이한 라운드가
  타수 자체는 훨씬 작기 때문에 최저타로 잘못 뽑히는 문제가 있었음. `strokesSum()`의 `cnt`(홀 수)가
  18인 라운드만 최고/최저 비교 대상에 포함하도록 수정(그런 라운드가 하나도 없으면 "18홀 완료
  기록 없음"으로 표시). 라운딩 횟수·연도별 통계 등 다른 지표는 그대로 전체 라운드를 포함함
  (요청 범위를 최고/최저로 한정).
- **종료 버튼 제거 + 메인 화면 뒤로가기로 종료되게 원복**: 실기기 테스트로 "설치된 PWA는
  `window.close()`로 절대 못 닫는다"는 게 확정됐으므로, 잘 작동하지도 않는 버튼을 없애는 대신
  안드로이드가 원래 잘 처리하는 방식을 살려둠 — 메인 화면(뒤로가기 스택 맨 위)에서 뒤로가기가
  눌리면 더 이상 `history.pushState`로 흡수하지 않고 그대로 흘려보내서, 갈 곳이 없어진 안드로이드가
  앱을 실제로 종료하게 함(서브 화면에서는 기존처럼 화면 이동만 되는 동작 그대로 유지 — 헤드리스로
  history.length가 서브 화면에서는 흡수되어 늘어나고 메인에서는 안 늘어나는 것까지 확인).
- `sw.js` 캐시 v14→v15, `APP_VERSION` 1.6.1→1.7.0.

## (l) 2026-09-21 — 스코어카드 사진 32건(2024년분) 히스토리 일괄 이전

`image_score/`의 SmartScore 스코어카드 캡처 32장(2024-01-06 ~ 2024-12-13)을 판독해 히스토리에 넣음.
앱 코드 변경 없음(그래서 `sw.js` 캐시/`APP_VERSION`은 그대로). 기존 78건(2025-01 ~ 2026-09)과 날짜가
겹치지 않아 그 앞쪽 기록을 채우는 작업이었음.

- **경로**: 기존 "엑셀에서 복원" 파이프라인 형식(`라운드정보`/`스코어` 시트)으로 xlsx를 만들어 폰에
  넣고 앱의 복원(병합, 중복 ID 건너뜀)으로 반영 → 결과 "32건 추가, 중복 0건". 통계는 라운드에서 매번
  계산되므로 라운딩 횟수 78→110, 연도별 표에 2024년 32건이 생긴 것으로 확인.
- **검산**: 카드의 각 칸은 Par 대비 +/- 값(나비 표시 = -1). 32건 모두 전반 합계와 총타수가 카드에
  인쇄된 값과 일치하는 것까지 스크립트로 확인(불일치 0건).
- **골프장명**: 영문 로고는 사용자가 준 한글 표기표대로 변환(라비돌·김포씨사이드·파크밸리·소피아그린·
  아난티서울). 로고만 있고 글자가 없거나 깨진 4건은 사용자 선택으로 임시 표기(`KCC`, `H로고(미확인)`,
  `미확인컨트리클럽`, `Q골프클럽`) — 앱에서 해당 라운드를 열어 골프장명만 고치면 통계에 즉시 반영됨.
- **조인 판정(추정)**: 우선 정렬 동반자가 있고 나머지 동반자 중 처음 보는 이름이 2명 이상이면 "조인"으로
  넣고 동반자는 우선 정렬 동반자만 남김(기존 조인 기록과 같은 관례). 그 외는 "일반"으로 카드의 동반자를
  모두 기록. 카드만으로는 확정할 수 없어 틀릴 수 있음 → 앱에서 라디오로 수정 가능.
- 퍼팅/Tee는 카드에 없어 빈 값(null)으로 둠. 라운드 ID는 `img_20260921_<파일명 끝 4자리>`(재처리해도
  병합 시 중복 없이 건너뜀). 전/후반 코스명은 카드 표기 그대로, `OUT`/`IN`뿐인 경우는 비워둠.
- 카드에 이름이 잘려 찍힌 동반자 1명(`ZHENGL.`)은 잘린 그대로 기록.

## (m) 2026-09-21 — 2023년분 스코어카드 33건 복원용 엑셀 생성

`image_score/`의 2023-02-04 ~ 2023-12-29 스코어카드 33장을 판독해, 앱에 직접 넣지 않고 사용자가
설정 → "엑셀에서 복원(병합)"으로 넣을 수 있는 `image_score/scorecard_restore_2023.xlsx`를 만듦
(기존 백업 파일의 시트/열 구성을 그대로 따름, 신규 33건만 포함하므로 병합해도 기존 기록은 그대로).

- 33건 모두 전반 합계·총타수가 카드 인쇄값과 일치하는지 스크립트로 검산(불일치 0건).
- 라운드 ID는 `img_20260921_<파일명 끝 4자리>`, 퍼팅/Tee는 빈 값, 시간은 24시간제로 변환(PM 01:38 → 13:38).
- 조인 여부는 카드의 동반자 구성으로 추정(우선 정렬 동반자만 남기는 관례) — 틀린 건 앱에서 수정.
- `image_score/`는 `.gitignore` 대상이라 이 파일은 저장소에 올라가지 않음.

## (n) 2026-09-21 — 히스토리: 연도 버튼 + 다중 항목 검색

- **연도 버튼**: 검색창 바로 아래에, 히스토리(완료된 라운드)에 실제로 있는 연도만 최신순으로 알약 모양
  버튼으로 표시(예: 2026 / 2025 / 2024 / 2023, 옆에 작은 글씨로 그 해의 건수). 누르면 그 해만 모아 보고,
  같은 버튼을 다시 누르면 해제. 선택한 해의 기록이 전부 삭제되면 자동으로 해제. 국내/해외 필터와 검색은
  연도와 함께(AND) 적용됨.
- **검색 범위 확장**: 골프장 이름만 보던 것을 골프장·전/후반 코스명·동반자·캐디명·한줄평·국가·국내/해외·
  조인/일반·날짜(`2024-03`, `2024년 3월` 둘 다)·시간까지 한 번에 검색. 띄어쓰기로 여러 단어를 쓰면 모든
  단어가 어딘가에 들어 있는 기록만 남음(예: `드림파크 홍길동`).
- 필터/검색 중에는 목록 위에 결과 건수를 표시, 같은 날짜 안에서는 시간 순으로 정렬.
- 헤드리스 Chrome에 가짜 데이터(실명 없음)를 넣어 연도 토글, 동반자·캐디·한줄평·날짜·조인·국가 검색,
  AND 검색, 결과 없음, 선택 연도 삭제 시 해제까지 확인. 폰의 실제 데이터는 사용하지 않음.
- `sw.js` 캐시 v37→v38 (`APP_VERSION`은 그대로).

## (o) 2026-09-22 — 2022년분 스코어카드 48건 복원용 엑셀 생성

`image_score/`의 2022-01-05 ~ 2022-12-04 스코어카드 48장을 판독해, 앱에 직접 넣지 않고 사용자가
설정 → "엑셀에서 복원(병합)"으로 넣을 수 있는 `image_score/scorecard_restore_2022.xlsx`를 만듦
(기존 백업 파일의 시트/열 구성을 그대로 따름, 신규 48건만 포함하므로 병합해도 기존 기록은 그대로).

- 기존 기록은 2023-02 ~ 2026-09만 있어 2022년 전체가 비어 있었음 — 이번 배치로 그 앞쪽 공백을 채움.
- 48건 모두 전/후반 Par합+스코어(+/-)합이 카드 인쇄 TOTAL과 일치하는지 스크립트로 검산(불일치 0건).
- 라운드 ID는 `img_20260922_<파일명 끝 4자리>`(재처리해도 병합 시 중복 없이 건너뜀), 퍼팅/Tee는 카드에
  없어 빈 값.
- 조인 판정(추정)은 기존 관례와 동일: 우선 정렬 동반자가 있고 나머지 동반자 중 처음 보는 이름이 2명
  이상이면 "조인"으로 넣고 동반자는 우선 정렬 동반자만 남김(48건 중 3건 해당), 그 외는 "일반"으로 카드의
  동반자를 모두 기록 — 카드만으로는 확정할 수 없어 틀릴 수 있음 → 앱에서 라디오/동반자란으로 수정 가능.
- 전반 9홀만 표시되고 후반이 빈 카드가 4건 있었음(9홀만 플레이) — 후반은 null(미기록)로 남겨 "0"과
  구분.
- 골프장명은 카드 로고 문구를 그대로 사용. 전/후반 코스명도 카드 표기 그대로 옮기되, 생성형(둘 다
  "OUT"처럼 실제 구분 없는 표기)인 2건은 비워둠.
- `image_score/`는 `.gitignore` 대상이라 이 파일은 저장소에 올라가지 않음.

## (p) 2026-09-23 — 2021년분 스코어카드 47건(원본 48건, 중복 1건 제외) 복원용 엑셀 생성

`image_score/`의 2021-01-16 ~ 2021-12-19 스코어카드 48장(SmartScore 앱 캡처본)을 판독해, 앱에 직접
넣지 않고 사용자가 설정 → "엑셀에서 복원(병합)"으로 넣을 수 있는 `scorecard_restore_2021.xlsx`를
만듦(기존 백업 파일의 시트/열 구성을 그대로 따름, 신규분만 포함하므로 병합해도 기존 기록은 그대로).
사용자 요청으로 `image_score/`(gitignore 대상) 안이 아니라 프로젝트 루트에 바로 생성함 — 다른
백업용 xlsx(`scorecard_backup_*.xlsx`)와 같은 자리, 마찬가지로 git에 추적되지 않는 미추적 파일임.

- 기존 기록은 2022-01 ~ 2026-09만 있어 2021년 전체가 비어 있었음 — 이번 배치로 가장 이른 공백을 채움.
- 48장 중 2장(촬영 시각만 다른 동일 라운드 재촬영본)이 완전히 같은 내용이라 1건은 중복 제외, 최종
  47건을 엑셀에 담음.
- 47건 모두 전/후반 Par합+스코어(+/-)합이 카드 인쇄 TOTAL과 일치하는지 스크립트로 검산(불일치 0건).
- 라운드 ID는 `img_20260923_<파일명 끝 4자리>`(재처리해도 병합 시 중복 없이 건너뜀), 퍼팅/Tee는 카드에
  없어 빈 값.
- 조인 판정(추정)은 기존 관례와 동일: 우선 정렬 동반자가 있고 나머지 동반자 중 기존 백업 전체(191건)에
  한 번도 없던 이름이 2명 이상이면 "조인"으로 넣고 동반자는 우선 정렬 동반자만 남김(47건 중 5건 해당),
  그 외는 "일반"으로 카드의 동반자를 모두 기록 — 카드만으로는 확정할 수 없어 틀릴 수 있음 → 앱에서
  라디오/동반자란으로 수정 가능.
- 같은 날 27홀(9홀 코스 3개 조합)을 쳐서 카드가 "9홀만 플레이 + 후반 전부 대시(-)"로 쪼개져 찍힌 경우가
  3일치(3쌍, 2021-06-20·08-28·11-20) 있었음 — 9홀짜리 카드는 후반 null(미기록)로 남기고 별개 라운드로
  기록(기존 관례와 동일). 사용자가 "9홀 미만(날씨로 중단한 라운드도 포함)까지 전부 넣어야 한다"고
  재확인해줘서 48장 전체를 다시 훑었으나, 이 배치에는 9홀 미만짜리(중도 포기) 카드는 없었음 — 위 3쌍
  외의 나머지는 모두 정상적으로 9홀 또는 18홀이 끝까지 채워진 카드였고, 47건 전체 검산(불일치 0건)도
  이 사실과 일치함.
- 골프장명은 처음엔 카드 로고 문구를 그대로 썼다가, 사용자가 준 영문 로고 → 한글 표기 대응표와
  날짜별 정정 지시를 받아 전부 갱신함: 소노(SONO, 설악산 인근) → 소노펠리체 델피노, 2021-08-15·
  08-14(원낭자/원화랑 코스) → 골프존카운티 화랑, 2021-06-18 → 골프클럽Q, 2021-05-08·05-07 →
  라비에벨, 2021-04-02 → 시그너스, 2021-03-31(코스는 동OUT/동IN 그대로) → 88CC, 2021-03-14 →
  360도, 2021-03-13·04-24·04-17(도고컨트리클럽) → 도고CC, 2021-02-10 → 블루헤런, 2021-02-05 →
  기흥CC, 2021-01-22(로고가 흐려 중원골프클럽으로 추정했던 것) → 한원컨트리클럽. 전/후반 코스명도
  카드 표기 그대로 옮기되, 생성형(둘 다 "OUT"/"IN"처럼 실제 구분 없는 표기)인 6건은 비워둠.
- `image_score/`는 `.gitignore` 대상이라 이 파일은 저장소에 올라가지 않음.

## (q) 2026-09-23 — 2022년분 기존 병합 데이터 중 9홀 라운드 4건의 후반 가짜 데이터 발견·수정

`image_score/`에 2022년 이미지 48장을 다시 넣고 "18홀이 안 되는 라운드도 빠짐없이 반영해달라"는
요청을 받아 전수 재검토하던 중, **이미 앱에 merge되어 있던 (o) 항목(2022년 48건)의 데이터 자체에
결함**을 발견함.

- **증상**: 48건 중 4건은 카드에 후반 9홀이 전부 대시(`-`, 미플레이)로 찍혀 있는 순수 9홀 라운드인데,
  기존 병합 데이터에는 후반 9홀이 `Par 4 / 타수 4 / 스코어 0`로 똑같이 채워져 마치 18홀을 다 친
  것처럼 저장돼 있었음(전반명은 카드 그대로, 후반명은 다행히 비어 있어 완전히 숨겨지진 않았음).
  전체 백업 191건의 `스코어` 시트를 이 "홀 10~18이 전부 Par4·타수4·스코어0" 패턴으로 스크립트
  검사한 결과, 이 결함은 이 4건에만 있고 다른 연도(2023~2026)에는 없음을 확인.
  - 2022-04-09 AM10:44 남여주골프클럽 가람(49타) — 후반 마루 미플레이
  - 2022-04-02 AM10:44 중원골프클럽 신라(51타) — 후반 고구려 미플레이
  - 2022-11-05 AM06:54 중원골프클럽 백제(47타) — 후반 신라 미플레이
  - 2022-09-30 AM06:27 중원골프클럽 백제(47타) — 후반 신라 미플레이
  - (넷 다 같은 날 다른 시각에 정상적인 18홀 라운드가 하나씩 더 있어서, 2021년분과 같은 "27홀 친
    날, 9홀+18홀로 쪼개져 찍힘" 패턴으로 보임 — 원래 세션이 이 패턴을 인식하지 못하고 9홀 카드도
    억지로 18홀 형식에 맞춰 채워 넣은 것으로 추정.)
- **수정 방식**: 기존 라운드 ID를 그대로 유지한 채 후반 9홀(가짜 데이터)만 제거하고 전반 9홀만 남긴
  `scorecard_restore_2022_수정.xlsx`(4건만 포함)를 프로젝트 루트에 새로 만듦 — "엑셀에서 복원(병합)"은
  같은 ID를 건너뛰기만 하고 덮어쓰지 않으므로, **사용자가 먼저 앱 히스토리에서 이 4건을 직접
  삭제한 뒤** 이 파일을 병합해야 정상 반영됨(그냥 병합만 하면 중복 ID라 무시되고 고쳐지지 않음).
- 파일명은 기존 `scorecard_restore_2022.xlsx`(이미 사용·삭제됨)와 헷갈리지 않도록 사용자 요청대로
  `scorecard_restore_2022_수정.xlsx`로 다르게 지음. `image_score/`가 아니라 프로젝트 루트에 직접
  생성(2021년분 때와 같은 이유 — 다른 백업 xlsx들과 같은 자리, git 미추적).
- 나머지 44건은 정상이라 손대지 않음.

## (r) 2026-09-23 — (o) 버그의 진짜 원인 특정, 2021·2022 restore 파일 둘 다 재생성

(q)에서 고친 4건이 애초에 왜 후반 9홀에 가짜 데이터가 채워졌는지 앱 코드(`index.html`)를 직접
추적해서 원인을 특정함 — restore 엑셀 문제가 아니라 **엑셀 import 로직 자체의 함정**이었음.

- **원인**: `doAiImport`류 복원 로직이 라운드 하나당 먼저 `newRound()`로 기본 객체를 만드는데, 이
  기본값이 `pars: Array(18).fill(4)`, `myScores: Array(18).fill(0)`(신규 라운드 화면에서 Par를
  미리 4로 채워두는 기능, (g) 항목 참고). 그다음 `스코어` 시트의 행을 훑으면서 그 홀 번호에 해당하는
  `pars[h]`/`myScores[h]`만 덮어씀. 즉 **9홀만 친 라운드라고 스코어 시트에 10~18번 홀 행을 아예
  안 넣으면, 그 홀들은 "안 건드려졌으니" `newRound()`의 기본값 Par4·타수0(=파)로 그대로 남아서
  마치 18홀을 다 친 것처럼 보임** — (o)에서 만들었던 restore 파일도, 이번에 처음 만든 2021년분
  restore 파일도 똑같이 이 함정에 걸려 있었음(9홀짜리 카드의 후반 행을 그냥 생략했었음).
- **올바른 표현법**: 안 친 홀은 행 자체를 생략하는 게 아니라, **홀 번호는 넣고 Par/타수/스코어(+/-)/
  퍼팅/Tee 값을 전부 빈 칸으로 명시적으로 써야** import 코드가 `par === '' || par == null` 조건에
  걸려 그 홀을 진짜 `null`(미기록)로 설정함. 이렇게 하면 `strokesSum()`이 그 홀을 카운트에서 빼서
  ① 합계가 실제로 친 홀까지만 더해지고(나머지를 0으로 채워 18홀처럼 보이는 일 없음), ② 통계 화면의
  최고/최저타 비교(`cnt === 18`인 라운드만 대상)에서도 자동으로 제외됨 — 두 요청 모두 앱에 이미
  있던 로직(전자는 `strokesSum`의 `cnt` 카운트 방식, 후자는 (l) 항목의 18홀 필터)이 그대로
  처리해주므로 앱 코드는 손대지 않고 **restore 엑셀 쪽의 "안 친 홀 표현 방식"만 고치면 됨**.
- **재생성**: `scorecard_restore_2021.xlsx`(9홀짜리 3건: 1909/2241/2919)과
  `scorecard_restore_2022_수정.xlsx`(9홀짜리 4건 전부)를 위 방식으로 다시 만듦 — 스코어 시트에
  10~18번 홀 행을 넣되 Par 이하 전부 빈 칸으로. 두 파일 다 홀별 합계 재검산 통과(불일치 0건),
  나머지(18홀 라운드) 데이터는 그대로.
- 앱 코드(`index.html`)는 이번에 건드리지 않음 — 문제는 항상 restore 엑셀 쪽 표현 방식이었음.

## (s) 2026-09-23 — 2020년분 스코어카드 40건 복원용 엑셀 생성

`image_score/`의 2020-01-04 ~ 2020-12-31 스코어카드 40장을 판독해, 앱에 직접 넣지 않고 사용자가
설정 → "엑셀에서 복원(병합)"으로 넣을 수 있는 `scorecard_restore_2020.xlsx`를 프로젝트 루트에 만듦
(2021년분과 같은 자리, git 미추적). 시트/열 구성은 기존 백업 파일과 같음.

- 40건 모두 전/후반 Par합+스코어(+/-)합이 카드 인쇄 TOTAL과 일치하는지 스크립트로 검산(불일치 0건),
  생성한 xlsx를 다시 읽어 라운드별 타수 합이 총타수와 같은지도 재확인. 40건 전부 18홀 카드라
  9홀 처리((r) 항목)는 해당 없음.
- 라운드 ID는 `img_2020_<파일명 끝 4자리>` — 파일명 날짜가 2021년분(`img_20260923_*`)과 같고 끝
  4자리 범위도 겹쳐서, 병합 때 우연히 중복으로 건너뛰는 일이 없도록 접두어를 따로 둠.
- 골프장명은 사용자가 준 영문 로고 → 한글 대응표대로 변환(마에스트로·신라·솔모로·대영베이스·스마트KU·
  아세코밸리·파인비치·필로스·속리산·롯데스카이힐부여·자유CC·안성베네스트 등). 날짜 지정분은 지시대로
  2020-05-15 → 한원컨트리클럽, 2020-03-21·01-18 → yJC골프클럽(카드 로고는 여주컨트리클럽),
  2020-02-01 → 스카이밸리, 2020-01-31 → 남춘천. 대응표에 없던 것은 카드 글자 그대로(드림파크·양평TPC·
  시그너스·캐슬파인·웨스트오션컨트리클럽·골프클럽Q·화성상록GC), `high1 CC`는 `하이원CC`로 표기.
- 전/후반 코스명은 카드 표기 그대로, 둘 다 OUT/IN뿐인 3건(아세코밸리 1, 자유CC 2)은 비워둠.
- 조인 여부: 이번에는 기존 백업 전체를 대조할 수 없어서(로컬에 백업 파일 없음) 추정 판정을 하지 않고
  40건 모두 "일반"으로 넣고 카드의 동반자를 전부 기록함 — 조인이었던 라운드는 앱에서 라디오만 바꾸면 됨.
- 퍼팅/Tee는 카드에 없어 빈 값.
