# why2korea 골프 스코어카드 — 설치 안내

다른 분(배우자, 동반자 등)에게 이 앱을 소개하고 설치해줄 때 그대로 따라 하면 되는 안내입니다.
서버나 회원가입이 전혀 없는 앱이라, 아래 순서대로 하면 몇 분 안에 끝납니다.

## 1. 접속 주소

```
https://why2korea.github.io/score_card/
```

이 주소를 상대방 휴대폰의 문자/카카오톡 등으로 보내주고, **크롬(Chrome) 브라우저**로 열게 하세요.
(안드로이드는 기본적으로 Chrome이 깔려 있습니다. 다른 브라우저에서도 열리긴 하지만, 아래 "홈
화면에 추가" 방식은 브라우저마다 위치가 조금씩 다릅니다.)

## 2. 홈 화면에 앱으로 설치하기

### 안드로이드 (Chrome)
1. 위 주소를 Chrome으로 엽니다.
2. 우측 상단 점 3개(⋮) 메뉴를 누릅니다.
3. **"앱 설치"** 또는 **"홈 화면에 추가"**를 누릅니다.
4. 확인을 누르면 홈 화면(앱 서랍)에 "골프스코어" 아이콘이 생깁니다.
5. 이후로는 이 아이콘으로 실행하면 주소창 없는 완전한 독립 앱처럼 열립니다.

### 아이폰 (Safari)
1. 위 주소를 반드시 **Safari**로 엽니다(크롬으로는 이 방식이 안 됩니다).
2. 하단 공유 아이콘(사각형에 위쪽 화살표)을 누릅니다.
3. **"홈 화면에 추가"**를 선택합니다.
4. 홈 화면에 아이콘이 생기고, 이후 그 아이콘으로 실행하면 됩니다.

## 3. 설치 후 첫 실행 시 해줄 것 (한 번만)

앱을 열면 설정(⚙️) 메뉴에 들어가서:
- **기본 사용자 (내 이름)**: 이 앱을 쓸 사람의 이름을 입력 (예: 배우자 이름). 스코어카드 요약에
  이 이름이 표시됩니다.
- **우선 정렬 동반자 이름**: 자주 같이 라운딩하는 특정 동반자 이름을 넣으면, 히스토리/통계에서
  그 사람이 항상 목록 맨 앞에 오도록 정렬해줍니다. 안 넣어도 됩니다(선택 사항).

이 두 이름은 **이 휴대폰의 브라우저 안에만 저장**되고, 외부 서버나 GitHub 어디에도 올라가지
않습니다.

## 4. 기존 라운딩 기록을 옮겨주고 싶다면 (선택)

이미 다른 폰(예: 내 폰)에 기록해둔 라운딩 데이터를 새로 설치한 사람에게도 넘겨주고 싶다면:

1. **원본 폰**에서: 설정 → **"엑셀로 내보내기"** → `scorecard_backup_YYYYMMDD_HHMM.xlsx` 파일이
   다운로드 폴더에 생성됨 → 카카오톡/이메일 등으로 그 파일을 새 폰에 전달.
2. **새로 설치한 폰**에서: 설정 → **"엑셀에서 복원"** → 전달받은 파일 선택 →
   - 새 폰이 완전히 비어있다면 **"전체 덮어쓰기"** 선택
   - 이미 새 폰에도 기록이 있다면 **"병합 (중복 ID 건너뜀)"** 선택(둘 다 안전하게 합쳐짐)

## 5. 알아두면 좋은 것

- **완전히 개인용 앱**입니다 — 로그인이 없고, 모든 기록은 각자 휴대폰 브라우저 안에만 저장됩니다.
  다른 사람이 설치해도 서로의 데이터가 섞이거나 공유되지 않습니다.
- **인터넷 없이도 대부분 동작**합니다(스코어 입력/저장/히스토리/통계 전부). 엑셀 내보내기/복원과
  캐디 평가 별점 등 일부 기능만 처음 한 번 인터넷이 필요합니다.
- 브라우저에서 "사이트 데이터 삭제"를 하거나 앱을 완전히 삭제하면 그 폰 안의 기록도 함께
  사라집니다. 오래 쓸수록 가끔 설정 화면에서 "엑셀로 내보내기"로 파일 백업을 남겨두시길
  권장합니다(설정 화면에 마지막 백업 날짜가 항상 표시됩니다).
- 종료는 메인 화면의 **"⏻ 종료"** 버튼으로 하고, 휴대폰 뒤로가기 버튼은 화면 이동에만 씁니다
  (실수로 앱이 꺼지지 않습니다).

---
*최초 작성: 2026-09-14. 앱이 업데이트되면 이 안내도 함께 갱신하는 게 좋습니다.*
